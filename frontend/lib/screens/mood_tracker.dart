// frontend/lib/tabs/mood_tab.dart
import 'package:flutter/material.dart';

class MoodTracker extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Mood and Progress Tracking'),
    );
  }
}