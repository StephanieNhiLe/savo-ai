// frontend/lib/tabs/safety_tab.dart
import 'package:flutter/material.dart';

class LocationSharing extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Circle Safety & Location Tracking'),
    );
  }
}